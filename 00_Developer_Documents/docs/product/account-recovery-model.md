# Account Recovery Model

- Hardware-backed root key wrap on device.
- Optional user-controlled recovery kit export with explicit confirmation.
- Biometrics unlock but are not the only recovery path.
- Recovery must not silently transfer identity without user confirmation ceremonies.
