# Observability

OpenTelemetry traces/metrics with privacy filter. Forbidden in logs: profile text, messages, exact location, evidence, secrets, raw attestations.

SLOs (staging targets): presence API p95 < 200ms; discovery p95 < 400ms; error rate < 1% excluding client faults.
